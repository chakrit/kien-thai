# Session checkpoint — 2026-06-08 · iter-8 outer-loop review (COMPLETE, uncommitted)

Outer-loop review (`spec/review-protocol.md`) of iteration-8 skill-clean outputs, under
**1-by-1** discipline. **All four items reviewed, batch executed, nothing committed yet.**
Tracked by `human-tasks-queue.md` "Phase 1 review" (iter-8 now done; 9/10 remain).

## State: done

Walk ①→④ all reviewed, all chakrit-clean: **no**. Verdicts for ① ② came from a prior
session (harvested from inline `output.md` edits); ③ ④ reviewed this session. Batch ran
per review-protocol steps 5–7:

- **Recorded** → `workspace/iteration-8/feedback.md` has the outer-loop section: a record
  block per output + the iter-8 dashboard (CLEAN-but-flawed 4/4 · recall-miss:coverage-gap
  ≈ 5:11 · 0 reads-flat).
- **Routed (collected, NOT applied** — step 6, `examples.md` untouched): research item R →
  `research-queue.md` "Discourse / composition axis"; three items → `human-tasks-queue.md`
  (Phase-1 updated, `reads-misstructured` decision, owed afters).
- **INDEX** → iter-8 flipped `pending → reviewed`.

## 🚨 Still do not regenerate iter-8

① ② verdicts live as inline edits in gitignored `output.md`. A regeneration run wipes
them; they are **not** re-derivable after that. The pairs are now also captured in the
tracked `feedback.md`, but the raw inline marks are not. Don't run generation on iter-8.

## Uncommitted working tree — one coherent commit pending chakrit's call

`docs/spec/review-protocol.md` (step-2 amendment, prior session) · `feedback.md` ·
`research-queue.md` · `human-tasks-queue.md` · `workspace/INDEX.md` · this note. All one
"iter-8 outer-loop review" commit on `main`. chakrit's call on when.

## Discussion points for next session

Surfaced this session, awaiting chakrit:

1. **Ambiguous classification — ①'s `หน้าต่างเวลา`.** Recorded as coverage-gap
   (noun-compound calque, "time window" rendered literally). But it could be a recall-miss
   against an existing calque slug. Confirmed `time-period` does NOT cover it (that slug is
   ใน-vs-ของ for periods). Decide at exemplar-promotion: new noun-compound-calque slug, or
   fold into an existing calque entry. Strongest exemplar candidate of the whole walk.
2. **Ambiguous classification — ③'s `กินทุนเงียบ`.** Recorded as coverage-gap
   (failed/opaque coinage; ทุน = capital/cost, NOT margin; not a calque). Could instead be
   craft `empty-intensifier`/empty-coinage. chakrit owes the "after" — no replacement was
   given. Pair can't graduate until he rewrites it.
3. **`reads-misstructured` / discourse axis (item R).** The big takeaway. Sentence-scoped
   skill + protocol are structurally blind to discourse cohesion — ② needs whole-piece
   restructure (Dao), ④ had an inter-sentence cohesion-marker error (`ก็มี` additive where
   contrast was meant). Protocol-flag decision is chakrit's; corpus-evidence half is queued
   for the agent. Gate any rule on attested native macrostructure per register.
4. **Owed "after" rewrites** (block pair-graduation): ② `amenity`-ทับศัพท์ + `พอ`/`ภาษาพูด`
   register slips + `และ` overuse; ④ flags A/B/C (`เหมือนเข้าบ้านเพื่อน…` incoherent simile,
   `ทอนเงิน` wrong verb-sense, `อะไรที่เรียกไม่ถูก` empty-profundity filler).

## Next session entry points

- Commit the batch (if not already), then **iter-9 / iter-10 outer-loop review** — same
  protocol, same 1-by-1 staging (one `output.md` at a time, state verdict format first).
- Or clear the discussion points above with chakrit before resuming the review.

## Ready pair (for the eventual exemplar pass)

`personal-blog` · before `ที่ยังเหมือนเดิมก็มี` → after `ที่ยังเหมือนเดิมไม่เปลี่ยนแปลงก็มี`.
Finding: additive `ก็มี` needs an explicit contrast-setup (mirror the prior sentence's verb
`เปลี่ยน` → `ไม่เปลี่ยน`) to be licensed; without it it mis-signals continuation.
